Medicrypt AI Frontend

Commands:

npm install
npm run dev

This scaffold uses Vite, React 19, TypeScript, and Tailwind CSS. Implement features one at a time and run the dev server with `npm run dev`.
